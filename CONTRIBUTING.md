# Contributing

Keep checks language-agnostic and grounded in repository evidence. Add or alter
a check only when it preserves the distinction between declared rules and
patterns inferred from multiple examples. Run `npm test` before submitting a
change.
